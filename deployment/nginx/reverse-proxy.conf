# ----------------------------
# IP-based deployment (default)
# ----------------------------
server {
    listen 80;
    server_name _;

    client_max_body_size 20m;

    # Frontend app
    location / {
        proxy_pass http://frontend:80;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Backend API
    location /api {
        proxy_pass http://backend:3003;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}

# -----------------------------------------------------
# Domain-based routing (enable after DNS is configured)
# -----------------------------------------------------
# server {
#     listen 80;
#     server_name domain.com www.domain.com;
#
#     location / {
#         proxy_pass http://frontend:80;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
#
# server {
#     listen 80;
#     server_name api.domain.com;
#
#     location / {
#         proxy_pass http://backend:3003;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#     }
# }
#
# --------- HTTPS notes ----------
# Option A: Certbot on host (recommended for VM)
# 1) Install certbot + nginx plugin on host
# 2) Issue certs for domain.com and api.domain.com
# 3) Mount cert paths into this container and add listen 443 ssl blocks
#
# Option B: Cloudflare (free SSL)
# 1) Point DNS to VM IP via Cloudflare
# 2) Set SSL/TLS mode to Full (strict preferred)
# 3) Keep origin on HTTP or install origin certs for end-to-end TLS
